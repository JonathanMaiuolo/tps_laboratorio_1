#include <stdio.h>
#include <stdlib.h>

float pedirKm(char mensaje []);
float pedirY (char mensaje []);
float pedir (char mensaje []);
float TarDebito(float);
float TarCredito(float);
float Bitcoin(float);
float PrecioUnitario(float,float);
float Diferencia(float,float);
void cargaForzada (void);
